version: '3.7'

services:
  nginx:
    image: nginx
    ports:
      - 8000:5000

networks:
  default:
    driver: macvlan
    driver_opts:
      mode: bridge
    ipam:
      config:
        -
          subnet: 192.168.20.0/24
          gateway: 192.168.20.1
