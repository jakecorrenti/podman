version: '3.7'

services:
  nginx:
    image: nginx
    ports:
      - 8000:5000

networks:
  default:
    driver: bridge
    driver_opts:
      com.docker.network.bridge.name: docker0
      com.docker.network.driver.mtu: 9000
